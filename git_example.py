#!/usr/bin/env python3

def multiply_numbers(a, b, c) : 
    """
    Multiply all three numbers
    """
    return a * b * c

# 1. extend multiply_numbers to allow an arbitrary number of parameters
# 2. investigate and resolve security exploit of multiply_numbers
# 3. make the implementation of multiply_numbers more performant
# 4. add documentation for multiply_numbers